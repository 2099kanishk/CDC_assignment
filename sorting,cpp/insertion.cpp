#include <bits/stdc++.h>
using namespace std;
vector<int> insertion_sort(vector<int> &nums , int n){
    for(int i = 0 ; i < n-1 ; i++){
        int j = i ;
        while ( j > 0 ; arr[j-1] >arr[j]){
            swap (arr[j-1] , arr[j]) ;
            j-- ;
        }
}
return nums ;
}