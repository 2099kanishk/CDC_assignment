#include <bits/stdc++.h>
using namespace std;
int union_inter(vector<int> &arr1 , vector<int> &arr2)
{
    sort(arr1.begin(), arr1.end());
    sort(arr2.begin(), arr2.end());
    int i = 0, j = 0;
    vector<int> ans ;
    while ( i < arr1.size() && j < arr2.size()){
        if(arr[i] < arr[j]){
            i++ ;
        }
        else if(arr[j] < arr[i]){
            j++ ;
        else{
            if(ans.empty )
        }        }
    }
} 
